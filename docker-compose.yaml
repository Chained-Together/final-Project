# version: '3.8'

# services:
#   app:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: app
#     environment:
#       - DB_HOST= ${DB_HOST}
#       - DB_PORT=5432
#       - DB_USER=${DB_USERNAME}
#       - DB_PASSWORD=${DB_PASSWORD}
#       - DB_NAME=${DB_NAME}
#       - PORT=${PORT}
#       - NODE_ENV=production
#     ports:
#       - '3000:3000'
#     volumes:
#       - ./src:/app/src

version: '3.8'
services:
  app:
    image: app-image # 이미지 이름 설정
    build:
      context: .
      dockerfile: Dockerfile # 명시적으로 Dockerfile 사용
    ports:
      - "3000:3000" # 애플리케이션 포트
    environment:
      DB_HOST: pg-3041fi.vpc-pub-cdb-kr.ntruss.com
      DB_PORT: 5432
      DB_USER: chainedtogether
      DB_PASS: aaaa4321!
      DB_NAME: loopfiy
    volumes:
      - ./src:/usr/src/app # 소스코드 공유를 위해 추가 (필요시)
  
  nginx-rtmp:
    build:
      context: ./nginx-rtmp # nginx-rtmp 디렉토리에서 빌드
    ports:
      - "1935:1935" # RTMP 스트리밍 포트
      - "8080:8080" # NGINX 관리 페이지 포트

